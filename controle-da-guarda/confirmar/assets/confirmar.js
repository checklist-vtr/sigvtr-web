const GuardConfirmPage=(()=>{
  const $=id=>document.getElementById(id);let rawToken='',operation='RETIRADA';
  function show(id){$(id).classList.remove('d-none')}function hide(id){$(id).classList.add('d-none')}
  async function enforceNoGuardCache(){
    try{if('serviceWorker' in navigator)navigator.serviceWorker.register('../../sw.js?v=1.18.5-rc1').catch(()=>{})}catch(_){}
    try{if(!('caches' in window))return;const names=await caches.keys();for(const name of names){const cache=await caches.open(name),requests=await cache.keys();for(const request of requests){const u=new URL(request.url);if(u.origin===location.origin&&u.pathname.includes('/controle-da-guarda/'))await cache.delete(request)}}}catch(_){}
  }
  function formatDateTime(iso){if(!iso)return '';const d=new Date(iso);return Number.isNaN(d.getTime())?'':new Intl.DateTimeFormat('pt-BR',{dateStyle:'short',timeStyle:'short'}).format(d)}
  function friendlyTokenError(msg){const s=String(msg||'');if(s.includes('QR_CODE_EXPIRADO'))return 'Este QR Code expirou. Solicite ao Comandante da Guarda um novo QR.';if(s.includes('QR_CODE_JA_UTILIZADO'))return 'Esta confirmação já foi utilizada.';if(s.includes('QR_CODE_INVALIDO'))return 'QR Code inválido ou não disponível.';return s||'Não foi possível validar esta confirmação.'}
  function showError(message){hide('loadingView');hide('formView');hide('successView');$('errorMessage').textContent=friendlyTokenError(message);show('errorView')}
  function showSuccess(data){hide('loadingView');hide('formView');hide('errorView');const isReturn=data.operacao==='DEVOLUCAO'||operation==='DEVOLUCAO';$('successTitle').textContent=isReturn?'Devolução confirmada':'Retirada confirmada';$('successDetails').innerHTML=isReturn?`<strong>VTR ${data.vtr?.prefixo||''}</strong><span>KM final: ${data.km??''}</span><span>Percorrido: ${data.kmPercorrido??0} km</span><span>${formatDateTime(data.confirmacaoEm)}</span>`:`<strong>VTR ${data.vtr?.prefixo||''}</strong><span>KM registrado: ${data.km??''}</span><span>${formatDateTime(data.confirmacaoEm)}</span>`;show('successView')}
  async function load(){rawToken=new URLSearchParams(location.search).get('token')||'';if(!rawToken){showError('QR_CODE_INVALIDO');return}try{history.replaceState(null,document.title,location.pathname);const data=await ApiService.publicPost('guardaPublicoTokenInfo',{token:rawToken},{timeout:15000,retries:0});operation=data.operacao||'RETIRADA';const isReturn=operation==='DEVOLUCAO';$('operationTitle').textContent=isReturn?'Confirmação de devolução':'Confirmação de retirada';$('confirmButtonText').textContent=isReturn?'Confirmar devolução':'Confirmar recebimento';$('vtrInfo').textContent=[data.vtr?.prefixo,data.vtr?.placa].filter(Boolean).join(' • ');$('driverInfo').textContent=[data.condutor?.postoGraduacao,data.condutor?.nomeGuerra].filter(Boolean).join(' ');$('requestInfo').textContent=formatDateTime(data.solicitacaoEm);hide('lastKmInfo');hide('initialKmInfo');if(isReturn){$('initialKmInfo').textContent=`KM inicial da retirada: ${data.kmInicial??0}`;show('initialKmInfo')}else if(data.ultimoKmConhecido!==null&&data.ultimoKmConhecido!==undefined){$('lastKmInfo').textContent=`Último KM conhecido no SIGVTR: ${data.ultimoKmConhecido}`;show('lastKmInfo')}hide('loadingView');show('formView');setTimeout(()=>$('kmInput').focus(),80)}catch(error){showError(error.message)}}
  async function confirm(event){event.preventDefault();const km=$('kmInput').value.trim();$('formAlert').classList.add('d-none');const btn=$('confirmButton');btn.disabled=true;btn.querySelector('.button-label').classList.add('d-none');btn.querySelector('.button-loading').classList.remove('d-none');try{const action=operation==='DEVOLUCAO'?'guardaPublicoConfirmarDevolucao':'guardaPublicoConfirmarRetirada';const data=await ApiService.publicPost(action,{token:rawToken,km},{timeout:20000,retries:0});showSuccess(data)}catch(error){$('formAlert').textContent=friendlyTokenError(error.message);$('formAlert').classList.remove('d-none')}finally{btn.disabled=false;btn.querySelector('.button-label').classList.remove('d-none');btn.querySelector('.button-loading').classList.add('d-none')}}
  function init(){$('confirmForm').addEventListener('submit',confirm);enforceNoGuardCache();load()}
  return{init};
})();
document.addEventListener('DOMContentLoaded',GuardConfirmPage.init);
