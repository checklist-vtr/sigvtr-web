const ApiService=(()=>{
  const CONFIG={baseUrl:"https://script.google.com/macros/s/AKfycbzuEEeAptN9MenKWY1oynX6c3gmGY7HgVXyGiGWGaoXeNOrmNNMUBCtXnutHVxJ13rv/exec",timeout:30000,retries:1,cachePrefix:"sigvtr_admin_api_v12017rc1:"};
  const memory=new Map(),pending=new Map();
  try{Object.keys(localStorage).filter(k=>k.startsWith('sigvtr_admin_api_')&&!k.startsWith(CONFIG.cachePrefix)).forEach(k=>localStorage.removeItem(k));localStorage.removeItem('sigvtr_admin_vehicles_v1');sessionStorage.removeItem('sigvtr_admin_vehicles_v1');}catch(_){}
  const sleep=ms=>new Promise(r=>setTimeout(r,ms));
  const stableParams=params=>Object.keys(params||{}).sort().reduce((o,k)=>(o[k]=params[k],o),{});
  const key=(action,params={})=>action+":"+JSON.stringify(stableParams(params));
  const storageKey=(action,params={})=>CONFIG.cachePrefix+key(action,params);
  const now=()=>typeof performance!=='undefined'&&performance.now?performance.now():Date.now();
  function logTiming(action,started,status){const elapsed=Math.max(0,Math.round(now()-started));console.debug('[SIGVTR API]',String(action||'unknown'),elapsed,'ms',status)}
  function friendlyError(error){const name=String(error&&error.name||''),msg=String(error&&error.message||'');if(name==='AbortError'||/aborted|signal is aborted/i.test(msg))return new Error('A conexão demorou mais que o esperado. Exibindo os últimos dados salvos e tentando novamente.');if(/404|Failed to fetch|NetworkError|Load failed/i.test(msg))return new Error('Servidor temporariamente indisponível. Exibindo os últimos dados salvos.');return new Error(msg||'Não foi possível atualizar os dados agora.');}
  function readCache(action,params={}){const k=key(action,params);if(memory.has(k))return memory.get(k);try{const raw=sessionStorage.getItem(storageKey(action,params));if(!raw)return null;const parsed=JSON.parse(raw);memory.set(k,parsed);return parsed}catch(_){return null}}
  function writeCache(action,params,data){const entry={savedAt:Date.now(),data};memory.set(key(action,params),entry);try{sessionStorage.setItem(storageKey(action,params),JSON.stringify(entry))}catch(_){}return entry}
  function buildUrl(action,params={}){const url=new URL(CONFIG.baseUrl);url.searchParams.set('action',action);url.searchParams.set('_ts',Date.now());Object.entries(params||{}).forEach(([k,v])=>{if(v!==''&&v!=null)url.searchParams.set(k,v)});return url}
  async function fetchRaw(options={},attempt=0,policy={}){const timeout=Number(policy.timeout)||CONFIG.timeout,retries=Number.isInteger(policy.retries)?policy.retries:CONFIG.retries,controller=new AbortController(),timer=setTimeout(()=>controller.abort(),timeout);try{const response=await fetch(CONFIG.baseUrl,{...options,signal:controller.signal,cache:'no-store'});if(!response.ok)throw new Error(`Falha na API: ${response.status}`);return await response.json()}catch(error){if(attempt<retries){await sleep(900*(attempt+1));return fetchRaw(options,attempt+1,policy)}throw friendlyError(error)}finally{clearTimeout(timer)}}
  function token(){try{return sessionStorage.getItem('sigvtr_admin_token')||''}catch(_){return ''}}
  async function request(action,data={},includeToken=true,options={}){const started=now(),payload={action,data};if(includeToken)payload.token=token();try{const json=await fetchRaw({method:'POST',headers:{'Content-Type':'text/plain;charset=utf-8'},body:JSON.stringify(payload)},0,options);if(!json.success){const err=new Error(json.message||'Falha no SIGVTR.');err.code=json.code||'';err.payload=json;throw err}logTiming(action,started,'ok');return json.data??json}catch(error){logTiming(action,started,'erro');throw error}}
  async function networkGet(action,params={}){const requestKey=key(action,params);if(pending.has(requestKey))return pending.get(requestKey);const task=(async()=>{try{let data;if(String(action).startsWith('admin')){data=await request(action,params,true)}else{const started=now(),controller=new AbortController(),timer=setTimeout(()=>controller.abort(),CONFIG.timeout);try{const response=await fetch(buildUrl(action,params),{method:'GET',signal:controller.signal,cache:'no-store'});if(!response.ok)throw new Error(`Falha na API: ${response.status}`);const json=await response.json();if(!json.success)throw new Error(json.message||'Falha no SIGVTR.');data=json.data??json;logTiming(action,started,'ok')}catch(error){logTiming(action,started,'erro');throw error}finally{clearTimeout(timer)}}writeCache(action,params,data);return {...data,_cache:{offline:false,savedAt:Date.now()}}}finally{pending.delete(requestKey)}})();pending.set(requestKey,task);return task}
  async function get(action,params={},options={}){const cached=readCache(action,params);if(options.forceNetwork)return networkGet(action,params);if(options.cacheOnly){if(!cached)throw new Error('Dados ainda não armazenados neste dispositivo.');return {...cached.data,_cache:{offline:true,savedAt:cached.savedAt}}}if(options.staleWhileRevalidate&&cached){networkGet(action,params).then(data=>{if(typeof options.onRevalidateSuccess==='function')try{options.onRevalidateSuccess(data)}catch(_){}}).catch(error=>{if(typeof options.onRevalidateError==='function')try{options.onRevalidateError(error)}catch(_){}});return {...cached.data,_cache:{offline:true,refreshing:true,savedAt:cached.savedAt}}}try{return await networkGet(action,params)}catch(error){if(error&&/^SESSION_/.test(error.code||''))throw error;if(cached)return {...cached.data,_cache:{offline:true,savedAt:cached.savedAt,error:error.message}};throw error}}
  async function post(action,data={},options={}){return request(action,data,true,options)}
  async function publicPost(action,data={},options={}){return request(action,data,false,options)}
  return{configure(v){CONFIG.baseUrl=String(v||'').trim()},get,post,publicPost,request,readCache,writeCache,friendlyError};
})();
